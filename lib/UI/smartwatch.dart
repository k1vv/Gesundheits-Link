import 'package:flutter/material.dart';

class WatchConnection extends StatelessWidget {
  const WatchConnection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("watch connection"),
    );
  }
}